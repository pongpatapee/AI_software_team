# Use central orchestration for v1

The v1 AI Software Team uses the PM Agent as a central orchestrator instead of starting with peer-to-peer or hierarchical orchestration. This keeps the first implementation easier to understand and debug while preserving peer-to-peer and hierarchical workflows as future learning branches once the baseline system works.
